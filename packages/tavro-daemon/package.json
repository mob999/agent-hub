{
  "name": "@tavro-ai/daemon",
  "version": "0.1.1",
  "description": "Tavro local daemon",
  "main": "dist/index.js",
  "bin": {
    "daemon": "dist/cli.js",
    "tavro-daemon": "dist/cli.js",
    "tavro-mcp": "dist/mcp-stdio.js"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "pnpm --filter @agent-hub/core build && pnpm --filter @agent-hub/config build && pnpm --filter @agent-hub/daemon build && tsup",
    "pack:dry-run": "npm pack --dry-run",
    "typecheck": "pnpm --filter @agent-hub/core build && pnpm --filter @agent-hub/config build && pnpm --filter @agent-hub/daemon build && tsc --noEmit"
  },
  "type": "module",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "ws": "^8.21.0"
  },
  "devDependencies": {
    "@agent-hub/daemon": "workspace:*",
    "tsup": "^8.5.1",
    "typescript": "^6.0.3"
  }
}
