{
  "name": "@tavro-ai/daemon",
  "version": "0.1.0",
  "description": "Tavro local daemon",
  "main": "dist/index.js",
  "bin": {
    "daemon": "dist/cli.js",
    "tavro-daemon": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "pnpm --filter @agent-hub/daemon build && tsup",
    "pack:dry-run": "npm pack --dry-run",
    "typecheck": "tsc --noEmit"
  },
  "type": "module",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "ws": "^8.21.0"
  },
  "devDependencies": {
    "@agent-hub/daemon": "workspace:*",
    "tsup": "^8.5.1",
    "typescript": "^6.0.3"
  }
}
